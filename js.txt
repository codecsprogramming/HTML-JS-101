Basics

    Declaring variables (var, let, const)
    Arrays: Creating and accessing arrays, Looping through arrays
    Objects
    Functions: Function declaration and expression, Arrow functions, Anonymous functions
    ( () => {
    } )();

Built-In Classes and Methods

    atob / btoa
    push / pop
    forEach, filter, map
    eval
    decodeURI, URLSearchParams
    String Methods: length, split, replace, trim, charAt, charCodeAt
    Template literals/string interpolation `${}`

DOM Basics

   What is DOM?
    Relationship between HTML, CSS, and JavaScript
    DOM tree structure (parent, child, siblings)

DOM Manipulation
    document.getElementById()
    document.querySelector()
    Changing text with innerHTML and innerText
    Modifying attributes with setAttribute / getAttribute

Window Object

    window properties and methods
        window.alert, window.confirm, window.prompt
        window.open
    window.location
        Navigating with location.href
        Reloading with location.reload()
        Extracting parts of the URL (protocol, hostname, path)

Event Handling

    window.onclick
    onmouseover
    onload

Extended Plan for HTML and JavaScript
Basics

    Variables
        Declaring variables (var, let, const)
        Scope (block, function, and global)
    Arrays
        Creating and accessing arrays
        Looping through arrays
        Multi-dimensional arrays
    Objects
        Key-value pairs
        Accessing and modifying properties
        Nested objects
    Functions
        Function declaration and expression
        Arrow functions
        Anonymous functions
        Function hoisting

Built-In Classes and Methods

    atob / btoa
        Encoding and decoding strings to/from Base64
    Array Methods
        push, pop, shift, unshift
        forEach, filter, map, reduce
        slice, splice
        indexOf, lastIndexOf
    String Methods
        length, split, replace, toUpperCase, toLowerCase
        trim, charAt, charCodeAt
        Template literals and string interpolation
    URL Handling
        decodeURI, encodeURI
        URLSearchParams for query string parsing and manipulation
    eval
        Executing JavaScript code dynamically
        Security risks and alternatives

DOM Basics

    What is the DOM?
        Relationship between HTML, CSS, and JavaScript
        DOM tree structure (parent, child, siblings)
    DOM Manipulation
        document.getElementById()
        document.querySelector() / querySelectorAll()
        Changing text with innerHTML and innerText
        Modifying attributes with setAttribute / getAttribute

Window Object

    window properties and methods
        window.alert, window.confirm, window.prompt
        window.open
    window.location
        Navigating with location.href
        Reloading with location.reload()
        Extracting parts of the URL (protocol, hostname, path)

Event Handling

    Mouse Events
        onclick, onmouseover, onmouseout
        onmousedown, onmouseup
    Keyboard Events
        onkeydown, onkeyup, onkeypress
    Load Events
        onload, onerror

Browser Interaction

    Debugging with console.log
    User Alerts
        Customizable alerts using alert, prompt, confirm
    Timing Functions
        setTimeout / setInterval
        Clearing intervals with clearTimeout, clearInterval

Web Storage

    cookie
    localStorage
    sessionStorage

HTTP Requests

    fetch
    Understanding request/response objects

Additional JavaScript Topics

    Template Literals
        Multiline strings
        Expression interpolation
    Destructuring
        Destructuring arrays and objects
        Default values during destructuring
    Spread and Rest Operators
        Merging arrays and objects
        Using rest parameters in functions
    Promises and Async/Await Basics
        Creating and consuming promises
        Handling asynchronous operations
    Error Handling in JavaScript
        try, catch, finally blocks
        Throwing and catching custom errors
    ES6 Modules
        Importing and exporting modules
        Default vs named exports
    Iterators and Generators
        Creating custom iterators
        Using yield for generator functions
    Object and Array Methods
        Object.keys, Object.values, Object.entries
        Array.from, Array.isArray
    Math Object
        Random number generation (Math.random())
        Rounding and precision (Math.round, Math.floor, Math.ceil)
    Working with Dates
        Creating and formatting dates
        Calculating date differences

    Spread and Rest Operators
    ES6 Modules (import / export)